# =============================================================================
# CUBO: ventasPorDocumento
# =============================================================================
# Este cubo representa el DETALLE DE VENTAS A NIVEL DOCUMENTO INDIVIDUAL.
# A diferencia de "reconciliacionVentasDiarias" (que pre-agrega por día),
# este cubo expone cada folio por separado, permitiendo análisis de
# devoluciones, facturación y comportamiento por cliente o vendedor.
#
# Fuente de datos: canastos_tfx.tfx_table_ventas
#
#  FILTRO DE VIABILIDAD:
#   Todas las medidas aplican internamente el filtro is_viable = true.
#   Los registros no viables (cancelados, corruptos) quedan en cero,
#   no se excluyen de la tabla, para preservar la integridad del conteo.
#
# TIPOS DE DOCUMENTO:
#   REM = Remisión   (venta normal)
#   FAC = Factura    (venta con comprobante fiscal)
#   DEV = Devolución completa
#   DV  = Devolución parcial
#
# Relación con otros cubos:
#   → No tiene joins declarados; es un cubo autónomo de análisis transaccional.
#   → "analiticaDevoluciones" lo consume como fuente principal.
#   → Para análisis agregado por día usar "reconciliacionVentasDiarias".
# =============================================================================

cubes:
  - name: ventasPorDocumento
    title: "Ventas por Tipo de Documento"
    description: "Ventas detalladas por tipo de documento (REM, FAC, DEV) para análisis de devoluciones y facturación"
    sql_table: canastos_tfx.tfx_table_ventas
    public: false # Solo para uso interno; se consume a través de vistas

    joins: [] # Sin relaciones a otros cubos; joins se declaran en las vistas

    # -------------------------------------------------------------------------
    # DIMENSIONES
    # -------------------------------------------------------------------------
    dimensions:
      # Llave primaria compuesta: folio + sucursal.
      # El mismo número de folio puede existir en distintas sucursales,
      # por lo que se necesitan ambos campos para garantizar unicidad.
      - name: id
        sql: "{CUBE}.venta || '-' || {CUBE}.sucursal"
        type: string
        primary_key: true
        title: "ID Venta"

      # Número de folio del documento. Puede repetirse entre sucursales.
      - name: numeroVenta
        sql: "{CUBE}.venta"
        type: string
        title: "Número de Venta"
        description: "Folio de la venta"

      # -----------------------------------------------------------------------
      # Dimensiones de tiempo
      # Se usa partition_date como fecha oficial del documento.
      # -----------------------------------------------------------------------

      - name: fechaVenta
        sql: "{CUBE}.partition_date"
        type: time
        title: "Fecha de Venta"

      - name: anio
        sql: "EXTRACT(YEAR FROM {CUBE}.partition_date)"
        type: number
        title: "Año"

      # Útil para ordenar cronológicamente en gráficas y tablas.
      - name: numMes
        sql: "EXTRACT(MONTH FROM {CUBE}.partition_date)"
        type: number
        title: "Mes (Num)"

      # Nombre legible para etiquetas en reportes y dashboards.
      - name: nombreMes
        sql: |
          CASE EXTRACT(MONTH FROM {CUBE}.partition_date)
            WHEN 1 THEN 'January'   WHEN 2  THEN 'February'  WHEN 3  THEN 'March'
            WHEN 4 THEN 'April'     WHEN 5  THEN 'May'        WHEN 6  THEN 'June'
            WHEN 7 THEN 'July'      WHEN 8  THEN 'August'     WHEN 9  THEN 'September'
            WHEN 10 THEN 'October'  WHEN 11 THEN 'November'   WHEN 12 THEN 'December'
          END
        type: string
        title: "Mes"

      # DOW: 0 = Sunday ... 6 = Saturday (convención PostgreSQL/Trino).
      - name: diaSemana
        sql: |
          CASE EXTRACT(DOW FROM {CUBE}.partition_date)
            WHEN 0 THEN 'Sunday'    WHEN 1 THEN 'Monday'    WHEN 2 THEN 'Tuesday'
            WHEN 3 THEN 'Wednesday' WHEN 4 THEN 'Thursday'  WHEN 5 THEN 'Friday'
            WHEN 6 THEN 'Saturday'
          END
        type: string
        title: "Día Semana"

      # -----------------------------------------------------------------------
      # Dimensiones operativas
      # -----------------------------------------------------------------------

      - name: sucursal
        sql: "{CUBE}.sucursal"
        type: string
        title: "Sucursal"

      # Tipo de documento; determina si el registro es venta o devolución.
      # REM/FAC = ingresos, DEV/DV = egresos (se restan en importeNeto).
      - name: tipoDocumento
        sql: "{CUBE}.tipo_doc"
        type: string
        title: "Tipo de Documento"
        description: "REM (Remisión/Venta), FAC (Factura), DEV (Devolución), DV (Devolución Parcial)"

      # Estado actual del documento.
      # CO = Completado (válido para análisis), CA = Cancelado.
      - name: estado
        sql: "{CUBE}.estado"
        type: string
        title: "Estado"
        description: "Estado del documento (CO=Completado, CA=Cancelado, etc)"

      # Código del cliente. Puede ser 'sys'/'SYS' en ventas de mostrador
      # sin cliente registrado; excluirlo con NOT IN al analizar cartera.
      - name: cliente
        sql: "{CUBE}.cliente"
        type: string
        title: "Cliente"

      # Serie del comprobante; útil para agrupar por punto de emisión fiscal.
      - name: serieDocumento
        sql: "{CUBE}.seriedocumento"
        type: string
        title: "Serie del Documento"

      # Texto libre. Puede contener motivo de devolución o agrupaciones
      # especiales como "TICKETS ANEXOS" que deben excluirse en algunos análisis.
      - name: observaciones
        sql: "{CUBE}.observ"
        type: string
        title: "Observaciones"

      # Vendedor asociado al documento; útil para análisis de desempeño.
      - name: vendedor
        sql: "{CUBE}.vend"
        type: string
        title: "Vendedor"

      # Flag que indica si el documento es válido para análisis.
      # Los no viables son cancelados, duplicados o registros corruptos.
      - name: esViable
        sql: "{CUBE}.is_viable"
        type: boolean
        title: "Es Viable"
        description: "Indica si la venta es válida para análisis"

    # -------------------------------------------------------------------------
    # MEDIDAS
    # Todas las medidas aplican is_viable = true internamente.
    # Los registros no viables contribuyen con 0, no se omiten.
    # -------------------------------------------------------------------------
    measures:
      # -----------------------------------------------------------------------
      # Importes (solo viables)
      # -----------------------------------------------------------------------

      # Suma de todos los documentos viables sin distinción de tipo.
      #  Incluye ventas Y devoluciones; no usar como métrica de ventas netas.
      - name: importeTotal
        sql: "CASE WHEN {CUBE}.is_viable = true THEN {CUBE}.importe ELSE 0 END"
        type: sum
        format: currency
        title: "Importe Total"
        description: "Suma total de importes de ventas VIABLES (excluye canceladas/corruptas)"

      # Solo documentos de venta (REM y FAC) viables.
      # Es la base para calcular tasas de devolución.
      - name: importeVentas
        sql: "CASE WHEN {CUBE}.is_viable = true AND {CUBE}.tipo_doc IN ('REM', 'FAC') THEN {CUBE}.importe ELSE 0 END"
        type: sum
        format: currency
        title: "Importe Ventas"
        description: "Solo ventas viables (REM y FAC), excluye devoluciones y canceladas"

      # Solo documentos de devolución (DEV y DV) viables.
      # Siempre positivo; restar de importeVentas para obtener neto.
      - name: importeDevoluciones
        sql: "CASE WHEN {CUBE}.is_viable = true AND {CUBE}.tipo_doc IN ('DEV', 'DV') THEN {CUBE}.importe ELSE 0 END"
        type: sum
        format: currency
        title: "Importe Devoluciones"
        description: "Solo devoluciones viables (DEV y DV)"

      # Ventas menos devoluciones: REM/FAC suman positivo, DEV/DV restan.
      # Es la métrica más representativa del ingreso real del período.
      - name: importeNeto
        sql: |
          CASE
            WHEN {CUBE}.is_viable = true AND {CUBE}.tipo_doc IN ('REM', 'FAC') THEN  {CUBE}.importe
            WHEN {CUBE}.is_viable = true AND {CUBE}.tipo_doc IN ('DEV', 'DV')  THEN -{CUBE}.importe
            ELSE 0
          END
        type: sum
        format: currency
        title: "Importe Neto"
        description: "Ventas menos devoluciones (solo viables)"

      # Descuentos aplicados sobre documentos viables.
      - name: descuentoTotal
        sql: "CASE WHEN {CUBE}.is_viable = true THEN {CUBE}.descuento ELSE 0 END"
        type: sum
        format: currency
        title: "Descuentos"

      # Impuestos registrados sobre documentos viables.
      - name: impuestoTotal
        sql: "CASE WHEN {CUBE}.is_viable = true THEN {CUBE}.impuesto ELSE 0 END"
        type: sum
        format: currency
        title: "Impuestos"

      # Precio de lista antes de descuentos e impuestos.
      - name: precioTotal
        sql: "CASE WHEN {CUBE}.is_viable = true THEN {CUBE}.precio ELSE 0 END"
        type: sum
        format: currency
        title: "Precio Total"

      # Costo de los artículos vendidos; base para calcular margen bruto.
      - name: costoTotal
        sql: "CASE WHEN {CUBE}.is_viable = true THEN {CUBE}.costo ELSE 0 END"
        type: sum
        format: currency
        title: "Costo Total"

      # -----------------------------------------------------------------------
      # Conteos (solo viables)
      # -----------------------------------------------------------------------

      # Total de documentos viables sin distinción de tipo.
      - name: conteoDocumentos
        sql: "CASE WHEN {CUBE}.is_viable = true THEN 1 ELSE 0 END"
        type: sum
        title: "# Documentos"
        description: "Número total de documentos viables"

      # Solo documentos de venta (REM y FAC) viables.
      - name: conteoVentas
        sql: "CASE WHEN {CUBE}.is_viable = true AND {CUBE}.tipo_doc IN ('REM', 'FAC') THEN 1 ELSE 0 END"
        type: sum
        title: "# Ventas"
        description: "Número de ventas viables (REM y FAC)"

      # Solo documentos de devolución (DEV y DV) viables.
      - name: conteoDevoluciones
        sql: "CASE WHEN {CUBE}.is_viable = true AND {CUBE}.tipo_doc IN ('DEV', 'DV') THEN 1 ELSE 0 END"
        type: sum
        title: "# Devoluciones"
        description: "Número de devoluciones viables (DEV y DV)"

      # Tasa de devoluciones precalculada en el cubo base.
      # Se castea a DOUBLE para evitar división entera en Trino.
      # NULLIF evita división por cero en períodos sin ventas.
      - name: porcentajeDevoluciones
        sql: |
          CAST(
            SUM(CASE WHEN {CUBE}.is_viable = true AND {CUBE}.tipo_doc IN ('DEV', 'DV')  THEN {CUBE}.importe ELSE 0 END) AS DOUBLE
          ) / NULLIF(
            CAST(SUM(CASE WHEN {CUBE}.is_viable = true AND {CUBE}.tipo_doc IN ('REM', 'FAC') THEN {CUBE}.importe ELSE 0 END) AS DOUBLE),
            0
          ) * 100
        type: number
        format: percent
        title: "% Devoluciones"
        description: "Porcentaje de devoluciones sobre ventas (solo viables)"

    # -------------------------------------------------------------------------
    # SEGMENTOS
    # -------------------------------------------------------------------------
    segments:
      # Muestra solo documentos de venta. Usar para análisis de ingresos puros.
      - name: soloVentas
        sql: "{CUBE}.tipo_doc IN ('REM', 'FAC')"
        title: "Solo Ventas (REM y FAC)"

      # Muestra solo documentos de devolución. Usar para auditoría de retornos.
      - name: soloDevoluciones
        sql: "{CUBE}.tipo_doc IN ('DEV', 'DV')"
        title: "Solo Devoluciones (DEV y DV)"

      # Excluye cancelados, duplicados y registros corruptos.
      # Recomendado activar en la mayoría de los análisis.
      - name: documentosViables
        sql: "{CUBE}.is_viable = true"
        title: "Solo Documentos Viables"

      # Solo documentos con estado 'CO' (completado).
      # Excluye pedidos pendientes que aún pueden modificarse.
      - name: documentosCompletados
        sql: "{CUBE}.estado = 'CO'"
        title: "Solo Completados"
